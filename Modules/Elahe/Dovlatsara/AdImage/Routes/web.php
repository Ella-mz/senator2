<?php

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| contains the "web" middleware group. Now create something great!
|
*/
use Illuminate\Support\Facades\Route;

//Route::prefix('adimage')->group(function() {
//    Route::get('/', 'AdImageController@index');
//});
//Route::prefix('admin')->middleware(['web', 'admin.auth'])
//    ->namespace('Modules\AdImage\Http\Controllers\Admin')->group(function() {
//
//        Route::get('deleteAdImage', 'AdImageController@deleteImage')->name('deleteAdImage.admin');
//
//    });
//Route::prefix('real-estate')->middleware(['web', 'realestate.auth'])
//    ->namespace('Modules\AdImage\Http\Controllers\Realestate')->group(function() {
//
//        Route::get('deleteAdImage', 'AdImageController@deleteImage')->name('deleteAdImage.realestate');
//
//    });
